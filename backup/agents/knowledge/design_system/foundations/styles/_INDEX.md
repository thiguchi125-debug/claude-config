# 様式の選び方（判断表）

> **design-studio Step 1 で参照を選ぶ前に、まずここで様式を1枚決める。**
> 様式を決めずに作り始めた紙面は、禁止則（紫グラデ・絵文字・浮きカード）を守っていても
> 「無難で弱い」に着地する。禁止は事故を防ぐだけで、造形の強さは作らない。

## 1. 読む相手と目的から引く（第一手）

| 誰に・何のために | 選ぶ様式 | 根拠 |
|---|---|---|
| 全世帯に配る市政報告・議会報告を**読ませたい** | `style_ink_saving_report` | 白70%で情報量が主役。自宅印刷のインクも持つ |
| 初対面の有権者に**顔と名前を覚えてほしい** | `style_photo_bold` | 顔＋一言。後援会・選挙物の正 |
| 自治会総会・敬老会で**格を保ちたい** | `style_washu_calm` | 明朝＋余白。急かさない紙面 |
| 子育て世帯・子どもに**楽しそうと思わせたい** | `style_bright_illustration` | イラストが主役。文字は削る |
| 若者・eスポーツに**攻めた印象を出したい** | `style_dark_lime` | 差し色ライム。迷ったら選ばない |
| 執行部・所管課・報道に**資料として信用させたい** | `style_gov_formal` | 装飾ゼロ・整列と数字で格を作る。**デジタル判型の第一候補** |
| すでに当事者の相手に**案件の経緯と数字を渡したい** | `style_newspaper_editorial` | 明朝＋段組で密度を許す。写真が少ない案件ほど効く |
| スクロール中の親指を**0.5秒で止めたい**（SNS画像） | `style_thumb_readable` | 主役文字72px以上・1画面1メッセージ。読ませるのは止めた後 |

## 2. 制約から引く（第二手・第一手と衝突したらこちらが勝つ）

| 制約 | 効く判断 |
|---|---|
| **使える実写真が無い** | `style_photo_bold` は不成立 → brand01（Warren 2020）のタイポ路線か `style_ink_saving_report` へ |
| **家庭用プリンタで刷る** | ベタ面の多い `style_dark_lime`・`style_bright_illustration` を外す → `style_ink_saving_report` |
| **文字量が多い（中面・レポート）** | 読者が**まだ関心を持っていない**（全世帯配布の1枚目）→ `style_ink_saving_report`（減らして白70%）。読者が**すでに当事者**（中面・裏面・案件説明）→ `style_newspaper_editorial`（入れ切って段で仕切る） |
| **イラスト素材が無い** | tri03（東大病院・公的清潔・単色）が最短。イラスト前提の様式を選ばない |
| **緊急・締切を伝える** | `style_washu_calm` を外す（明朝と余白が緊急度を殺す） |
| **高齢者が主読者** | `style_dark_lime` を外す（可読性） |
| **紙でなく画面で読ませる** | 用途で割れる。**資料・スライド・PDF・Notion** → `style_gov_formal`／**SNSの小判型**（Instagram 4:5・9:16・1:1） → `style_thumb_readable`。残る5様式は全て紙前提で、明朝細字の `style_newspaper_editorial`・`style_washu_calm` は画面でつぶれる |
| **SNS画像を印刷物に流用したい／その逆** | どちらも不可。`style_thumb_readable` はRGBのライム前提＋2階層設計、紙は級数設計が別物。**作り直す**（判型が変わったら様式から選び直す） |
| **市民に配る印刷物**である | `style_gov_formal` を外す（市の公式広報と誤認される。官公庁ブルー系は様式ごと使わない） |
| **写真が1枚も無い** | `style_gov_formal`（rep10＝写真0で図版60%）か `style_newspaper_editorial`（rep01・rep02＝文字多め紙面）。写真の過小配置で誤魔化さない |

## 3. 決めたら次にやること

1. 選んだ様式カード（`style_*.html`）を**自分でRead**する — 配色・フォント・装飾語彙・NGをそのまま実装制約にする
2. カード内「相性の良い参照」の ID を `references/_INDEX.md` で引き、**参照画像を自分でRead**する
3. 参照の `_karte.md` の5軸数値（級数ジャンプ率／色数／図形語彙／写真・図版占有率／イラスト点数）を
   print-layout-architect への指示にそのまま目標値として渡す（Step 4-B でこの5軸が採点される）

## 4. 「該当なし」のとき

無理に既存8枚へ寄せない。次の順で処理する:

1. 制約表（2章）で**外す様式**だけ先に決める
2. 残った中から一番近いものを**ベース**にし、変更点を1〜2点に絞る（配色だけ／書体だけ）
3. 出来が良ければ Step 7-1 で**新しい様式カードとして昇格**させる（`_INDEX.md` の1章にも行を足す）

様式を3点以上いじった時点で、それは既存様式ではなく新様式。カード化して残す。

## 5. 空白（次に埋めるもの）

- ~~官公庁フォーマル系~~ — **2026-08-25 追加**（`style_gov_formal`）。デジタル庁デザインシステムの**文法だけ**を借り、
  **色は借りない**（官公庁ブルーは公式刊行物との誤認を生む → `#0f3d27` / `#1f5a3a` に置換。tri11・tri12 の政党色置換と同じ扱い）
- ~~新聞体エディトリアル系~~ — **2026-08-25 追加**（`style_newspaper_editorial`）。`style_ink_saving_report` との使い分けは
  **白面積ではなく読者の関心度**で決める（未関心＝減らす／当事者＝入れ切る）。判断表は2章の「文字量が多い」行

次に空いているもの:

- **ポスター系の様式** — `references/poster/` が空のままで採寸元が無い。参照収集が先（`references/_INDEX.md` の収集の空白1）
- ~~デジタルの小判型（Instagram 4:5・9:16）~~ — **2026-08-25 追加**（`style_thumb_readable`）。
  可読性の床（主役72px／支え44px／注釈26px）とセーフゾーンは short-video-image-designer と**同一値**にしてある。
  片方だけ直さない
