---
name: タスク管理刷新（2026-05-02）
description: タスクDB＋プロジェクトDBの「3DBの嘘」を解消、5+2ビュー追加、壊れた既存ビューを📦リネーム、ハブページ刷新
type: project
originSessionId: 9a9e7ee5-7eca-4eb3-ace0-44c9502a7dc3
---
# タスク管理刷新 2026-05-02

## 旧状態の問題

1. **嘘の3DB記述**: ハブページが「✅タスク（日次）／📋タスク（継続）／🗂️プロジェクト」の3DBと謳っていたが、実体はタスクDB＋プロジェクトDBの2つ（2026-04-24統合済み）
2. **壊れたビュー**: タスクDB「プロジェクト別」「ボード」、プロジェクトDB「アクティブ」が `期限 date_is exact` の空フィルタや存在しない `is_group "In Progress"` で表示が空
3. **実用ビューなし**: 「今日の優先タスク」「期限切れ」「inbox」などが無く、25件以上のタスクが整理されない
4. **過剰プロパティ**: 24個のプロパティ（訪問軒数・連絡ツール・昇格判定タグ等）が常時表示で見辛い

## 実施内容

### タスクDB に新規ビュー5本
- 🔥 締切順（未完了）— 期限ASC、過去日(期限切れ)が上
- 🆕 直近追加 — フィルタ無し全件
- 🗂️ プロジェクト別ボード — GROUP BY プロジェクト
- 📋 ステータス別ボード — GROUP BY ステータス
- 📅 カレンダー — 期限ベース

### プロジェクトDB
- ▶️ アクティブ（新）新規作成（旧の壊れフィルタを回避）
- 📋 ステータス別ボード 追加
- 旧「アクティブ」→「📦 旧アクティブ（壊れ・削除可）」リネーム

### 壊れたビューのリネーム（手動削除誘導）
- タスクDB「プロジェクト別」→「📦 旧プロジェクト別（壊れ・削除可）」
- タスクDB「ボード」→「📦 旧ボード（壊れ・削除可）」

### ハブページ刷新
- 嘘の「3DB」記述削除→「タスクDB＋プロジェクトDB の2つ」明記
- 「タスクが流れ込む5経路」（nichijo / iken / citizen-inquiry-responder / policy-update / 直接登録）を明示
- 各ビューの使い分けガイド掲載
- Notion API ステータス型フィルタ制約の警告を明記（ユーザーに手動でDone/Archive除外を追加してもらう）

## 制約事項（要・手動操作）

Notion view-DSL がステータス型プロパティのフィルタ書き込みをサポートしないため、以下は **草川さん側で Notion UI から手動設定が必要**:
1. 各ビューに「ステータス ≠ Done, ≠ Archive」フィルタ追加（一度設定すれば保存）
2. 📦 旧○○（壊れ・削除可）と命名された4ビューを削除（タスクDB×2、プロジェクトDB×1、＋プロジェクトDB「ボード（重複・削除可）」）

詳細は feedback_notion_dsl_status_filter_limitation.md 参照
