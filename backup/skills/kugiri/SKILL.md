---
name: kugiri
description: "草川たくや（亀山市議会議員）の作業環境で、長くなったセッションを安全に区切って /clear するためのスキル。【区切る側】「区切り」「区切って」「kugiri」「引き継ぎメモ」「セッション切る」「clearしたい」「ここで一旦切る」、およびトークン監視フックが⛔🔴を出した直後に必ず起動する。【再開する側】/clear 直後の「再開」「続きから」「さっきの続き」「引き継ぎ読んで」でも起動し、~/.claude/handoff/LAST.md の「次の一手」から着手する（通常はSessionStartフックが本文を自動注入するので読み直し不要）。作りかけの成果物を保存し、次セッションが1回のReadで追従できる引き継ぎメモを ~/.claude/handoff/LAST.md に書き、草川に /clear を促すところまでを1パスで行う。メモは20行以内・1500字以内に必ず収める（長い引き継ぎは節約の目的を潰すため）。※タスクのTodoist登録→task-add、日次の活動記録→nichijo、情報の保存先振り分け→smart-intake が正で本スキルは反応しない。"
---

# kugiri — セッションを安全に区切る

## なぜ必要か（実測）

トークン消費は **文脈サイズ × 呼び出し回数** でほぼ決まる。文脈は減らないので、区切る以外に効く手段はない。

2026-09-01〜02 の実測（セッション `e02e916f`）:

- 24.5時間・227回で **100.1M トークン**。うち**出力はわずか 0.90M（0.9%）**
- 残り99.1%は「同じ会話を毎回送り直した分」
- 文脈500K超で回した**40%の呼び出しが、コストの67%**
- 文脈988Kでは `ls` 1回もブログ1版も**同じ約1M**

区切らなかったことが唯一の出血源だった。作り直しの回数ではない。

**区切りの代償は起動時固定費の約73K だけ。文脈が300Kを超えていれば、切れば必ず得。**

## 起動したらまずやること

現在の文脈サイズを実測して、草川に判断材料を出す。

```bash
python3 ~/.claude/scripts/ctx_now.py
```

- **300K未満** … 「まだ切らなくてよい」と伝えて終了（無駄な区切りもコスト）
- **300K以上** … 下の Step 1 へ進む

## Step 1: 作りかけの成果物を先に保存する（最優先）

**引き継ぎメモより先にこれをやる。** メモは作り直せるが、文脈の中にしか無い原稿は /clear で消える。

1. このセッションで作った未保存の原稿・データを洗い出す
2. 保存先は CLAUDE.md 保存先マップに従う
   - AI下書き → `~/.claude/projects/-Users-kusakawatakuya/drafts/`
   - スキル生成物 → `~/outputs/`
3. **Notion・ブログ・SNS原稿を保存するなら安全ゲートは省略しない**
   （content-fact-checker → content-risk-reviewer）。
   ゲート未通過のものは「未通過」と明記してローカルの drafts/ にだけ置く。

## Step 2: 引き継ぎメモを書く

**2か所に書く。** 並行セッションが同時に区切ると `LAST.md` は相互に上書きされるため（既知の `gate.py` 承認記録の消し合いと同じ構造）、実体はタイムスタンプ付きで残す。

```bash
mkdir -p ~/.claude/handoff/archive
# 1. 実体（消えない。案件名を入れる）
#    ~/.claude/handoff/archive/<YYYY-MM-DD_HHMM>_<案件名>.md
# 2. 直近ポインタ（SessionStartフックが読む。常に上書き）
cp ~/.claude/handoff/archive/<いま書いたファイル> ~/.claude/handoff/LAST.md
```

`LAST.md` が別セッションに奪われても `archive/` に残るので復旧できる。

**厳守：20行以内・1500字以内。** 長い引き継ぎは次セッションの起動固定費を膨らませ、区切りの効果を打ち消す。経緯・議論の再現は書かない。**次の一手が分かる最小限だけ**。

```markdown
# 引き継ぎ <YYYY-MM-DD HH:MM>

## いま何をしているか
<1〜2文。案件名と目的>

## どこまで進んだか
- <完了した事実だけ。3〜5項目>

## 次の一手
1. <具体的な動作。ファイル名・DB名・コマンドまで書く>

## 保存済みの成果物
- <絶対パス>（ゲート通過/未通過を明記）

## 注意
- <ハマりどころ・未解決の判断だけ。無ければ「なし」>
```

書いたら文字数を必ず検算する。

```bash
wc -l -m ~/.claude/handoff/LAST.md
```

超えていたら削る。削る優先順は「経緯 → 背景説明 → 完了項目の細部」。

## Step 3: 草川に区切りを促す

以下の形で、短く提示する。

```
区切りの準備ができました。
　文脈 <N>K → 次セッションは 73K から始まります（このまま100回続けると +<N/10>M）
　保存済み: <パス>
　引き継ぎ: ~/.claude/handoff/LAST.md

/clear を実行してください。
そのあと「再開」とだけ入力すれば、私が引き継ぎメモを読んで続きから始めます。
```

**草川が /clear を実行するまでが区切り。** 促して終わりにしない。

## 再開側（次セッションの動作）

**通常は読む必要がない。** SessionStart フック `handoff_notice.py` が、24時間以内の `LAST.md` の本文を新セッション冒頭へ自動注入する（起動直後の文脈73Kで `cat` を1回走らせると73K払うため、本文を直接入れるほうが100倍以上安い）。

注入が無いとき（24時間超・別セッションに上書きされた等）だけ、明示的に探す。

```bash
ls -t ~/.claude/handoff/archive/ | head -5
```

いずれにせよ「次の一手」から着手する。**前セッションの経緯を掘り返さない**（それをやると区切った意味が消える）。過去ログが要るときだけ、必要な箇所を `sed -n` で抜く。

## 📌 恒久ガードルール

1. **メモは20行・1500字以内。** 超えたら削る。長い引き継ぎは区切りの効果を打ち消す。
2. **成果物の保存はメモより先。** 順序を入れ替えない。
3. **300K未満では区切らない。** 起動固定費73Kの払い損になる。
4. **発信物を保存するなら安全ゲートは省略しない。** 区切りを急ぐことは省略の理由にならない。
5. **実体は archive/ にタイムスタンプ付きで残し、LAST.md はそのコピー。** 並行セッションに上書きされても復旧できるようにする。
6. **再開時に前セッションのjsonlを読み返さない。** 引き継ぎメモ（自動注入）だけで着手する。
7. **handoff配下をテストで破壊しない。** 動作確認は必ず別ディレクトリのダミーで行う（2026-09-02、実テストで別セッションの引き継ぎを消す事故を起こした）。
